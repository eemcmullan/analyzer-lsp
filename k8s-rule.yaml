- message: Deployment is using image with latest tag. Use a version or snapshot tag.
  ruleID: k8s-deployment-0001
  description: "Check for image tags"
  category: potential
  effort: 2
  when:
    yaml.k8sResourceMatched:
        imageTag: "latest"
